import DynamicComponentProps from "./DynamicComponentProps";

interface DynamicCanvasProps extends DynamicComponentProps {
    verticalRange: string[]
    horizontalRange: string[]
}

export default DynamicCanvasProps;
